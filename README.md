# flutterHomeAssignment

## Home assignment for mobile dev

For proper weather service work don't forget to add your `appId` in `weather_service.dart`

## Localhost

For localhost use check `api_constants.dart`, change `_useLocal` to `true`
